[project]
name = "mishka-archivist"
version = "0.1.0"
description = "Extracts facts from daily logs"
requires-python = ">=3.11"
dependencies = [
    "httpx",
    "apscheduler",
    "loguru",
    "pydantic",
    "python-dotenv",
    "fastapi",
    "uvicorn",
    "aio-pika"
]
