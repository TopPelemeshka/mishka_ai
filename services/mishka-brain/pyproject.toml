[project]
name = "mishka-brain"
version = "0.1.0"
description = "Core decision making service for Mishka AI"
requires-python = ">=3.11"
dependencies = [
    "langgraph",
    "fastapi",
    "uvicorn",
    "pydantic"
]
