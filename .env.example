# === Main Config ===
# Token of your Telegram Bot
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# === Admin Panel ===
# Enable to bypass Telegram signature check (set initData="dev" in frontend)
DEV_MODE=false
# Telegram User ID who has FULL ADMIN access (Single ID)
SUPERADMIN_ID=12345678
# Comma-separated list of Telegram User IDs who have READ-ONLY access
VIEWER_IDS=11223344
# Master Password for double-auth in Admin Panel
ADMIN_PASSWORD=change_me_securely
# Secret key for JWT generation (can be any string)
JWT_SECRET=super_secret_string

# === LLM Provider ===
# Internal URL for LLM Provider (used by other services)
LLM_PROVIDER_URL=http://mishka-llm-provider:8000/v1/chat/completions
# Proxy for LLM requests (External -> Gemini)
# Format: http://user:pass@host:port
LLM_PROXY=http://127.0.0.1:12334

# Gemini API Keys
GEMINI_API_KEY=your_primary_key
GOOGLE_API_KEYS=key1,key2,key3

# Fallback Default Model (used if no dynamic config found)
LLM_MODEL=gemini-3-flash-preview

# === Database ===
# Postgres (Memory Service)
POSTGRES_USER=mishka
POSTGRES_PASSWORD=secret
POSTGRES_DB=mishka_db
POSTGRES_HOST=mishka-memory
POSTGRES_PORT=5432

# Redis (Cache & PubSub)
REDIS_HOST=redis
REDIS_PORT=6379
# If using password
# REDIS_PASSWORD=

# Qdrant (Vector DB)
QDRANT_HOST=mishka-memory
QDRANT_PORT=6333

# RabbitMQ (Message Broker)
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
RABBITMQ_HOST=rabbitmq

# === Access Control (Legacy Bot) ===
# Telegram Chat ID (Group or User) allowed to talk to the bot
ALLOWED_GROUP_ID=12345678

# === Initiative System ===
# Fallback Threshold (used if no dynamic config found)
INITIATIVE_THRESHOLD=70
# Bot username (without @) to detect mentions (Hard Rule, keep in env)
BOT_USERNAME=mishka_bot